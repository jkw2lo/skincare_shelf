name = "shelf-life-barcode"
main = "src/index.js"
compatibility_date = "2025-09-01"

# Secrets are set with `wrangler secret put NAME` — never commit them.
#   RAKUTEN_ID          free at webservice.rakuten.co.jp
#   NAVER_ID            free at developers.naver.com  (Korean products)
#   NAVER_SECRET
#   BARCODE_LOOKUP_KEY  paid, optional
#   UPCITEMDB_KEY       optional; falls back to the free trial endpoint
